DOWNLOAD_VARIANT=${DOWNLOAD_VARIANT:-sp1024}
DOWNLOAD_TRAIN_SHARDS=${DOWNLOAD_TRAIN_SHARDS:-1}
SKIP_PIP_INSTALL=${SKIP_PIP_INSTALL:-0}
GPU_COUNT=${GPU_COUNT:-2}
RUN_PURPOSE=${RUN_PURPOSE:-smoke}
RUN_HARDWARE=${RUN_HARDWARE:-2x5090}
WANDB_ENABLE=${WANDB_ENABLE:-1}
WANDB_PROJECT=${WANDB_PROJECT:-parameter-golf}
WANDB_GROUP=${WANDB_GROUP:-experiments}
WANDB_TAGS=${WANDB_TAGS:-runpod,2x5090,smoke}
VOCAB_SIZE=${VOCAB_SIZE:-1024}
NUM_LAYERS=${NUM_LAYERS:-11}
MODEL_DIM=${MODEL_DIM:-512}
NUM_HEADS=${NUM_HEADS:-8}
NUM_KV_HEADS=${NUM_KV_HEADS:-4}
MLP_MULT=${MLP_MULT:-3}
TIE_EMBEDDINGS=${TIE_EMBEDDINGS:-1}
ITERATIONS=${ITERATIONS:-3}
WARMUP_STEPS=${WARMUP_STEPS:-0}
MAX_WALLCLOCK_SECONDS=${MAX_WALLCLOCK_SECONDS:-120}
GRAD_ACCUM_STEPS=${GRAD_ACCUM_STEPS:-1}
TRAIN_BATCH_TOKENS=${TRAIN_BATCH_TOKENS:-65536}
TRAIN_SEQ_LEN=${TRAIN_SEQ_LEN:-2048}
TRAIN_LOG_EVERY=${TRAIN_LOG_EVERY:-1}
VAL_LOSS_EVERY=${VAL_LOSS_EVERY:-0}
VAL_BATCH_SIZE=${VAL_BATCH_SIZE:-65536}
TORCH_PROFILER_ENABLE=${TORCH_PROFILER_ENABLE:-1}
TORCH_PROFILER_WAIT_STEPS=${TORCH_PROFILER_WAIT_STEPS:-1}
TORCH_PROFILER_WARMUP_STEPS=${TORCH_PROFILER_WARMUP_STEPS:-1}
TORCH_PROFILER_ACTIVE_STEPS=${TORCH_PROFILER_ACTIVE_STEPS:-1}
MUON_BACKEND_STEPS=${MUON_BACKEND_STEPS:-5}
MUON_WD=${MUON_WD:-0.02}
MUON_MOMENTUM_WARMUP_START=${MUON_MOMENTUM_WARMUP_START:-0.85}
MUON_MOMENTUM_WARMUP_STEPS=${MUON_MOMENTUM_WARMUP_STEPS:-500}
EVAL_STRIDE=${EVAL_STRIDE:-64}
BIGRAM_VOCAB_SIZE=${BIGRAM_VOCAB_SIZE:-16384}
BIGRAM_DIM=${BIGRAM_DIM:-64}
XSA_LAST_N=${XSA_LAST_N:-4}
ROPE_DIMS=${ROPE_DIMS:-32}
LN_SCALE=${LN_SCALE:-1}
LATE_QAT=${LATE_QAT:-1}
INT5_MLP=${INT5_MLP:-1}
INT5_ALL=${INT5_ALL:-0}
TTT_ENABLED=${TTT_ENABLED:-0}
